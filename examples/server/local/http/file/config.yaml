server_type: 1 # http server
server_name: trellis
address: 0.0.0.0:8000

http_server_config:
  groups:
    - path: /v1
      handlers:
        -
          method: POST
          path: ""
          uses: ["use1"]
  handlers:
    - method: POST
      path: "/v1"
      uses: [ "use1" ]
      handler: ""

router_config:
  registry_config:
    prefix: /trellis
  components:
    -
      service:
        domain: trellis
        name: componentb
        version: v1
      options:
        server: haha